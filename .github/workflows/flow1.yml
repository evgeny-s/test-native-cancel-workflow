name: Sleep for 30 seconds

on:
  push:

jobs:
  test-timeout:
    name: Sleep job
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Sleep for 90 seconds
        run: sleep 900
